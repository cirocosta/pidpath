project_name: pidpath

before:
  hooks:
    - go mod tidy

builds:
  - id: linux-build
    env: [CGO_ENABLED=0]
    goos: [linux]
    goarch: [amd64]
  - id: darwin-build
    env: [CGO_ENABLED=1]
    goos: [darwin]
    goarch: [amd64]
